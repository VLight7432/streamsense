// Types et constantes partagés pour StreamSense backend / extension

const STREAM_STATUS = {
  RUNNING: 'running',
  STOPPED: 'stopped',
  DEGRADED: 'degraded',
};

module.exports = {
  STREAM_STATUS,
};
